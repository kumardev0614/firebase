import 'package:flutter/cupertino.dart';

class ConfirmVideoScreen extends StatelessWidget {
  const ConfirmVideoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Confirm Video Screen"),
    );
  }
}
